// import { useRouteContext } from "node_modules/@tanstack/react-router/dist/cjs/useRouteContext.d.cts"

import { useRouteContext } from "@tanstack/react-router"


export const SOC = () => {

    // const {wellDetails} = useRouteContext({from: '/_userAuthed/well/$wellID'})

    // console.trace(wellDetails)
    // // const {user, flatNodes} = useRouteContext({from: '/_userAuthed/well/asdf'})

    // return <div>
    //     SOC
    //     {JSON.stringify(wellDetails)}
    // </div>
return null
}